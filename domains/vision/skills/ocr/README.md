# OCR / Document Vision

See `SKILL.md` for OCR and document-vision rules. Use representative page resolution and batch sizes in smoke tests and keep high-resolution document data in bounded-memory pipelines.