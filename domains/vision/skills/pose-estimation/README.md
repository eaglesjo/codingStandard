# Pose Estimation

Use keypoint/landmark workflows with explicit keypoint schema, image size, augmentation, and reproducible evaluation. Control resolution, batch, feature-map memory, workers, and prefetching. Use task-appropriate metrics such as PCK/AP and standard checkpoint, Early Stopping, Resume, and ablation policies.