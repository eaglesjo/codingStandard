# Language Resources

This directory stores localized Markdown documentation used by the installer.

- `en/` — English templates (default)
- `ko/` — Korean templates

The installer copies the selected language into the standard project entrypoint filenames such as `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, and the `LLM/` documentation files.
