# codingStandard 1.8.1

## Purpose

Strengthen the hardware-specific LLM QLoRA/PEFT validation path after the 1.8.0 strategy-routing release.

## Changes

- Add a reproducible QLoRA/PEFT validation guide for fresh Colab CUDA runtimes.
- Update the Colab validation notebook to call `prepare_model_for_kbit_training()` before LoRA attachment.
- Validate NF4 4-bit loading, PEFT trainable-parameter fraction, forward execution, and adapter export.
- Synchronize the shared runtime `STANDARD_VERSION` with `VERSION`.

## Validation scope

CI continues to validate resource-aware strategy routing without downloading models. The Colab notebook remains the hardware-specific integration test and requires a CUDA GPU runtime.
