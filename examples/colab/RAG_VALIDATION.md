# RAG Colab validation

Run `rag_validation.ipynb` in a fresh Google Colab runtime. It validates dense embedding retrieval, evidence-bearing prompt assembly, and small-model text generation. Required CI validates notebook JSON and runs the dependency-free RAG smoke test; real model execution belongs in Colab.
